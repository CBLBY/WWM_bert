import pandas as pd
import numpy as np

#df = pd.read_csv("./WWM_bert/datas/Train_DataSet.csv")
# a = np.asarray([[1], [2]])
# b = np.asarray([[3], [4]])
# c = np.concatenate((a, b), axis=0).reshape((2, 2))
# print(c)
# d = np.random.rand(2, 2)
# print(d)
# e = np.concatenate((c, d), axis=1)
# print(e)
k = 5
for i in range(k):
    print(i)
